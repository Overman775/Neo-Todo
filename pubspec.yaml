name: todolist
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^4.1.2
  sqflite: ^1.3.0+1
  path_provider: ^1.6.9
  flutter_svg: ^0.17.4
  pedantic: ^1.9.0
  flutter_neumorphic: ^2.0.1
  equatable: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - assets/images/